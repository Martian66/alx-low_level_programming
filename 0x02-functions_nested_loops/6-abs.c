#include "main.h"
#include <stdio.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @c: is the int that will be used as an argument for function
 * Return: Always 0
 */
int _abs(int)
{
if (c > 0 || c == 0)
{
return (c);
}
else

return (c * -1);
}
